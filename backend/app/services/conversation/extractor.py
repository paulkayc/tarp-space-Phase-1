"""
Extraction pass — ARCHITECTURE.md Section 3.1
LLM call #1 per user message.
Input:  user message + current mandate state + mandate field schema
Output: structured mandate_delta JSON (only newly extractable fields)
All extracted fields tagged source="inferred".
"""
# TODO: implement in Phase 1 conversation sprint
